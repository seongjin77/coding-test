function solution(arr){
    let answer;
    let m = arr.length;
    let n = arr[0].length;
    

    for(let i=0; i<m; i++){
        for(let j=0; j<n; j++){
            if(arr[i][j]){
               //만약 첫번째 수 보다 작은것을 체크한다
               // 작은게 없으면 카운트 1 증가
               // 하나라도 반례가 있으면 for문 탈출

            }
        }// 각 학생수
    }// m번의 시험


    console.log(answer);
};